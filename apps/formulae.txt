# Homebrew formulae to install.
# One per line. Lines starting with `#` and blank lines are ignored.
# Anything from `#` to end-of-line is treated as a comment.

# === Development tools ===
pyenv
rust
# rustup        # conflicts with `rust`, pick one
node
pnpm
bun
go
gradle
maven
llvm@21         # large download
gemini-cli

# === Databases / services ===
mysql
mysql-client
redis

# === Command line utilities ===
wget
vim
aria2
p7zip
autojump
ack
tmux
jq
ripgrep
pandoc
hexo
bash            # newer than the macOS built-in bash 3.2
curl
bc
bind            # dig / nslookup
netcat
